# FormCraft v1.5 — To-Do

Legend: `[ ]` not started · `[x]` done · `[-]` skipped/deferred

---

## 1. Database Migrations

### 1.1 New tables
- [ ] Create `question_groups` table
- [ ] Create `question_variants` table
- [ ] Create `form_sessions` table
- [ ] Create `variant_assignments` table with unique constraint on `(session_id, question_group_id)`
- [ ] Create `question_events` table
- [ ] Create `optimization_runs` table

### 1.2 Indexes
- [ ] Index `question_variants(question_group_id)`
- [ ] Index `variant_assignments(session_id)`
- [ ] Index `variant_assignments(question_group_id)`
- [ ] Index `question_events(session_id)`
- [ ] Index `question_events(variant_id)`
- [ ] Index `question_events(event_type, variant_id)` — for analytics queries
- [ ] Index `form_sessions(form_id)`
- [ ] Index `optimization_runs(question_group_id, ran_at)`

### 1.3 Alter existing tables
- [ ] Add `question_group_id uuid nullable` to `fields` with FK to `question_groups(id) on delete set null`
- [ ] Add `session_id uuid nullable` to `responses` with FK to `form_sessions(id) on delete set null`

### 1.4 RLS policies
- [ ] `question_groups` — owner read/write (via form ownership)
- [ ] `question_variants` — owner read/write (via group → form ownership)
- [ ] `form_sessions` — public insert (anon can create), owner read
- [ ] `variant_assignments` — public insert (anon), owner read
- [ ] `question_events` — public insert (anon), owner read
- [ ] `optimization_runs` — owner read only (written by service role)

---

## 2. Backend — Session & Assignment API

### 2.1 `POST /api/v1/forms/:id/sessions`
- [ ] Route file and handler
- [ ] Parse `device_type`, `referrer`, `metadata` from body
- [ ] Insert row into `form_sessions`
- [ ] Return `{ session_id }`
- [ ] Validate form exists and is published before creating session

### 2.2 `POST /api/v1/forms/:id/sessions/:sessionId/assign`
- [ ] Route file and handler
- [ ] Fetch all active `question_groups` for this form
- [ ] For each group, check if assignment already exists in `variant_assignments` → return it if so
- [ ] Weighted random selection from active variants using `traffic_weight`
- [ ] Write new assignments to `variant_assignments`
- [ ] Handle DB unique conflict on concurrent assignment (re-read and return existing)
- [ ] Return full variant content: `{ assignments: [{ question_group_id, variant_id, variant: { title, type, config, logic } }] }`
- [ ] Return empty `assignments: []` gracefully when form has no groups

---

## 3. Backend — Event Tracking API

### 3.1 `POST /api/v1/sessions/:sessionId/events`
- [ ] Route file and handler
- [ ] Accept `events[]` array in body
- [ ] Validate each event has `event_type` ∈ allowed set
- [ ] Accept either `variant_id` or `field_id` per event (not required to have both)
- [ ] Bulk insert into `question_events`
- [ ] Return `204` on success, no body
- [ ] Rate-limit or size-cap the batch (max 50 events per call)

---

## 4. Backend — Question Groups & Variants API

### 4.1 Groups CRUD
- [ ] `GET /api/v1/forms/:id/groups` — list all groups for a form (auth required)
- [ ] `POST /api/v1/forms/:id/groups` — create group, body: `{ label, optimization_goal?, strategy? }`
- [ ] `PATCH /api/v1/forms/:id/groups/:groupId` — update label, goal, strategy
- [ ] `DELETE /api/v1/forms/:id/groups/:groupId` — delete group; set `fields.question_group_id = null` for linked fields

### 4.2 Variants CRUD
- [ ] `GET /api/v1/forms/:id/groups/:groupId/variants` — list variants
- [ ] `POST /api/v1/forms/:id/groups/:groupId/variants` — create variant, body: `{ variant_label, title, type, config?, logic? }`
- [ ] `PATCH /api/v1/forms/:id/groups/:groupId/variants/:variantId` — update content or `is_active`
- [ ] `DELETE /api/v1/forms/:id/groups/:groupId/variants/:variantId` — delete variant; block if it's the last active one

### 4.3 Weight reset
- [ ] `POST /api/v1/forms/:id/groups/:groupId/reset-weights` — set all active variant weights to `1.0` (equal distribution)

---

## 5. Backend — Analytics API

### 5.1 `GET /api/v1/forms/:id/groups/:groupId/analytics`
- [ ] Route file and handler
- [ ] Aggregate from `question_events`: impressions, answers, skips per variant
- [ ] Compute `answer_rate`, `avg_answer_time_ms`, `abandonment_rate`
- [ ] Read current `traffic_weight` from `question_variants`
- [ ] Determine `is_winner` flag: variant with highest weight and weight > 0.5
- [ ] Return per-variant stats array
- [ ] Include `last_optimized_at` from latest `optimization_runs` row for this group

---

## 6. Optimization Engine

### 6.1 Scoring function (shared utility)
- [ ] Implement `scoreVariant({ answer_rate, avg_duration_ms }) → number`
- [ ] Implement exploration floor normalization with edge case for 6+ variants
- [ ] Implement statistical guard: skip rebalance if all scores within 0.03 of each other

### 6.2 Optimization job
- [ ] Create Supabase Edge Function or pg_cron job entry point
- [ ] Loop over all active `question_groups` across all forms
- [ ] Fetch event counts per variant (impressions, answers, avg duration)
- [ ] Skip group if any variant < 100 impressions → write `optimization_runs` with `action = 'skipped_low_data'`
- [ ] Compute scores and new weights
- [ ] Skip if no change within threshold → write `action = 'skipped_no_change'`
- [ ] Update `traffic_weight` on `question_variants`
- [ ] Write `optimization_runs` snapshot with `action = 'rebalanced'`

### 6.3 Scheduling
- [ ] Configure cron trigger — every 15 minutes
- [ ] Ensure job runs with service role key (bypasses RLS for writes)
- [ ] Add basic logging / error capture to `optimization_runs.notes`

---

## 7. Renderer Changes

### 7.1 Session management
- [ ] On form load: check `localStorage` for `fc_session_{form_id}`
- [ ] If no session: `POST /api/v1/forms/:id/sessions` → store returned `session_id`
- [ ] Call `POST /api/v1/forms/:id/sessions/:sessionId/assign` to get variant assignments
- [ ] Merge variant content into field list: replace `title`, `type`, `config`, `logic` for fields with matching `question_group_id`
- [ ] Handle assignment API error gracefully: fall back to canonical field content, still render form

### 7.2 Session lifecycle
- [ ] On form restart (existing button): clear `fc_session_{form_id}` from localStorage, create new session
- [ ] On form completion: mark session — `PATCH /api/v1/sessions/:sessionId` with `{ completed_at }` or include in submit call

### 7.3 Event emission
- [ ] Add event buffer (in-memory array) to `FormRenderer`
- [ ] Track `shown_at` timestamp when each question becomes active
- [ ] Emit `shown` event when question activates
- [ ] Emit `answered` event on confirmed answer (with `duration_ms`)
- [ ] Emit `skipped` event when optional question is skipped
- [ ] Emit `backtracked` event when user presses Back
- [ ] Flush buffer via `POST /api/v1/sessions/:sessionId/events` every 5 seconds if non-empty
- [ ] Flush on `visibilitychange` to hidden using `navigator.sendBeacon`
- [ ] Flush on form completion before submit
- [ ] Clear buffer after successful flush

---

## 8. Builder UI

### 8.1 Field card badge
- [ ] Show "A/B" badge on field cards that have an active question group

### 8.2 Variant editor in field panel
- [ ] "Turn into experiment" button on any field in the editor panel
  - Creates a question group
  - Converts the current field into Variant A
- [ ] Variant list within the field editor panel
  - Show each variant: label, title preview, is_active toggle
- [ ] "Add Variant" button — opens a variant form (label, title, type, config)
- [ ] "Edit Variant" — inline editing of title, type, config, logic for each variant
- [ ] "Delete Variant" — with confirmation; blocked if last active variant
- [ ] "Reset to equal weights" button
- [ ] Show current traffic weight per variant (read-only, e.g. "60% traffic")
- [ ] Show impression count per variant (read-only, fetched from analytics endpoint)

### 8.3 Experiments tab (per form)
- [ ] New tab route: `/forms/:id/experiments`
- [ ] Add "Experiments" link to form nav alongside "Builder" and "Responses"
- [ ] Experiments page shell component
- [ ] List all question groups for the form
- [ ] Per group: expandable section showing variant analytics table
  - Columns: Variant, Impressions, Answer Rate, Avg Time, Traffic Share, Status
  - Status badge: "Collecting data" / "Optimizing" / "Winner" based on impression count and weight
- [ ] Empty state when form has no experiments yet
- [ ] "Last optimized" timestamp per group
- [ ] Refresh button to re-fetch analytics

---

## 9. Shared Schema Package

- [ ] Add `QuestionGroup` Zod type to `packages/schema`
- [ ] Add `QuestionVariant` Zod type
- [ ] Add `FormSession` Zod type
- [ ] Add `QuestionEvent` type with `event_type` enum
- [ ] Add `VariantAssignment` type
- [ ] Update `Field` type to include optional `question_group_id`

---

## 10. QA & Edge Cases

- [ ] Test: renderer falls back to canonical field when assign API fails
- [ ] Test: session re-use on page reload (same variant shown)
- [ ] Test: new session after restart (new variant may differ)
- [ ] Test: variant deactivated mid-session — in-progress sessions unaffected
- [ ] Test: all variants deactivated — falls back to canonical field, no 500
- [ ] Test: concurrent assignment requests for same session+group — only one assignment written
- [ ] Test: optimization skipped when variant impressions < 100
- [ ] Test: exploration floor reduces correctly when n_variants ≥ 6
- [ ] Test: weight reset API returns weights to equal
- [ ] Test: event batch with mixed `variant_id` and `field_id` entries
- [ ] Test: `sendBeacon` flush on tab close (manual browser test)
- [ ] Test: adding a new variant to an active experiment freezes optimization until new variant clears 100 impressions
